global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: opshub_backend
    static_configs:
      - targets:
          - backend:9876

  - job_name: opshub_agent
    file_sd_configs:
      - files:
          - /etc/prometheus/file_sd/*.json
